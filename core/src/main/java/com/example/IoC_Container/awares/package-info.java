package com.example.IoC_Container.awares;

/**
 * ApplicationContextAware: Allows access to the Spring ApplicationContext where the bean is defined.
 * BeanNameAware: Allows access to the bean’s name in the Spring container.
 * ApplicationEventPublisherAware: Allows beans to publish events using the ApplicationEventPublisher.
 * BeanClassLoaderAware: Provides access to the class loader used to load the bean classes.
 * BeanFactoryAware: Provides access to the BeanFactory to allow low-level bean management.
 * LoadTimeWeaverAware: Allows access to the weaver for load-time weaving (used with AspectJ).
 * MessageSourceAware: Provides access to the MessageSource for message resolution and internationalization.
 * NotificationPublisherAware: Allows access to the JMX notification publisher.
 * ResourceLoaderAware: Allows access to the ResourceLoader to load resources.
 * ServletConfigAware: Provides access to the ServletConfig in web-aware Spring contexts.
 * ServletContextAware: Provides access to the ServletContext in web-aware Spring contexts
 */